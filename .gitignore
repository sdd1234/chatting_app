# ── Node (plain-ws, react-client, websocket-client)
node_modules/
dist/
.vite/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# ── Java / Maven / Spring Boot (spring-server)
target/
*.class
*.jar
!**/lib/*.jar
.mvn/wrapper/maven-wrapper.jar
hs_err_pid*

# ── 환경변수 / 시크릿 (운영 전환 시 추가될 값들)
.env
.env.local
.env.*.local
*.pem
*.key
*.p12
*.jks
*.crt

# ── IDE
.idea/
.vscode/
*.iml
*.iws
*.ipr

# ── OS
.DS_Store
Thumbs.db
desktop.ini

# ── 런타임 컨테이너 볼륨 (있다면)
data/
postgres-data/
minio-data/
redis-data/

# ── 사용자 업로드 파일 (코드 아님 — 데이터는 git 제외, 도커는 볼륨 마운트)
spring-server/uploads/
